# Google Sheets API Configuration
# To use Google Sheets fetching, you need to:
# 1. Create a Google Cloud Project
# 2. Enable Google Sheets API
# 3. Create a Service Account and download the JSON key
# 4. Share your Google Sheet with the service account email
# 5. Export the values below

# Path to your Google Service Account JSON key file
export GOOGLE_SERVICE_ACCOUNT_KEY_PATH="./service-account-key.json"

# Your Google Sheets document ID (from the URL)
# Example: https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
export GOOGLE_SHEETS_ID="your-spreadsheet-id-here"

# Sheet names mapping (optional)
# Format: CSV_FILENAME:SHEET_NAME,CSV_FILENAME2:SHEET_NAME2
# If omitted, all sheets/tabs will be auto-discovered and saved as slugified CSV filenames.
# Example: heroes:Heroes Data,items:Items Data
export SHEETS_MAPPING=""

# Strict Configuration Mode
# When set to "true", the fetch-config command will exit with error code if fetching fails
# This is useful in CI/CD to ensure configuration is always fetched from Google Sheets
# Default: false (uses example CSV files as fallback)
export STRICT_CONFIG_MODE="false"
